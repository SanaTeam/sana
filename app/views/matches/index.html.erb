<div class="container">
    <div class ="d-flex justify-content-between flex-wrap">
        <div class="w-30 ml-3">
            <%= image_tag("chatting.svg", size: "200x200")%>
        </div>
        <div class="w-50 heading mr-auto p-5">
            <h1 class="d-inline">Matches</h1>
        </div>
    </div>
</div>
<%= render 'posts_display'%>

<hr>
<h5 class="text-muted">Swipe left or right to deny or begin a match with a user!</h5>
<div class="mt-3">
<%= link_to(user_path(current_user.id, section: "match"), class: "text-reset") do %>
    <span class="text-blue"><%= fa_icon "handshake-o", text: "View Your Matches" %></span>
<% end %>
</div>
<br>
<br>
<div class="d-flex justify-content-center" data-controller="match">
  <%= render 'card', post: Post.all.sample %>
</div>