<nav class="navbar navbar-expand-lg navbar-light bg-light">
<!-- <nav class="navbar navbar-expand-lg navbar-light bg-light"> -->
  <%= link_to(root_url, class: "navbar-brand") do %>
    <%= fa_icon "heartbeat", text: "Sana" %>
  <% end %>
  <button class="navbar-toggler navbar-brand" type="button" data-toggle="collapse" data-target="#navbar-sana" aria-controls="navbar-sana" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbar-sana">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <%= link_to(posts_path, class: "nav-link navbar-brand") do %>
          Forum
        <% end %>
      </li>
      <li class="nav-item">
        <%= link_to(matches_path, class: "nav-link navbar-brand") do %>
          Match
        <% end %>
      </li>
      <li class="nav-item">
      <%= link_to(users_path, class: "nav-link navbar-brand") do %>
        Users
      <% end %>
    </li>
    </ul>
    <ul class="navbar-nav">
      <% if logged_in? %>
        <% if current_user.admin %>
          <li class="nav-item ">
            <%= link_to(rails_admin_path, class: "nav-link navbar-brand") do %>
              Admin
            <% end %>
          </li>
        <% end %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle navbar-brand" href="#" id="navbar-dropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            <%= current_user.first_name %>
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbar-dropdown">
            <%= link_to(user_path(current_user), class: "dropdown-item") do %>
              Profile
            <% end %>
            <%= link_to(session_path(session), class: "dropdown-item", method: :delete) do %>
              Log Out
            <% end %>
          </div>
        </li>
      <% else %> 
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle navbar-brand" href="#" id="navbar-dropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
            Guest
          </a>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbar-dropdown">
            <%= link_to(new_session_path, class: "dropdown-item") do %>
             Log In
            <% end %>    
            <%= link_to(new_user_path, class: "dropdown-item") do %>
              Sign Up
            <% end %>
          </div>
        </li>
      <% end %>
    </ul>
  </div>
</nav>